import './errorContainer.scss';
import { ErrorImage, ErrorText, ErrorButton } from '../';

const ErrorContainer = () => {
	// RECIBIR Y ENVIAR DATOS ACA
	return (
		<div>
			<ErrorImage />
			<ErrorText />
			<ErrorButton />
		</div>
	);
};

export default ErrorContainer;
